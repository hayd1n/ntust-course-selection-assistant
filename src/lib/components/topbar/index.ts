import TopbarHeader from "./topbar-header.svelte";
import TopbarSpace from "./topbar-space.svelte";
import TopbarSubtitle from "./topbar-subtitle.svelte";
import TopbarTitle from "./topbar-title.svelte";
import Topbar from "./topbar.svelte";

const Root = Topbar;
const Space = TopbarSpace;
const Header = TopbarHeader;
const Title = TopbarTitle;
const Subtitle = TopbarSubtitle;

export { Root, Space, Header, Title, Subtitle };
